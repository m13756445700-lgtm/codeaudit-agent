# No live credentials belong in this repository.
CODEAUDIT_REPOSITORY_ROOT=/data/repositories
CODEAUDIT_OUTPUT_ROOT=/data/audit-runs

# Fill locally; never commit .env. Use a model supported by the configured Codex backend.
CODEAUDIT_CODEX_MODEL=
CODEAUDIT_OCTOBUS_TOKEN=
